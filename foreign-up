set -e

if test -z "$1"
then
	prefix="$HOME/Gits/0xfb"
else
	prefix="$1"
fi

test -d foreign || mkdir foreign

cd foreign

if test -d mkenv
then
	(cd mkenv; git pull)
else
	git clone "$prefix/mkenv"
fi
