set -e

base="$(dirname "$0")"

fgn="$base/foreign"
bld="$base/bld"

bld="$(cd "$bld" && pwd)"
fgn="$(cd "$fgn" && pwd)"
tcn=clang

(cd "$base/src" \
	&& make -j 5 -f gnu.mk bld="$bld" foreign="$fgn" toolchain="$tcn")
